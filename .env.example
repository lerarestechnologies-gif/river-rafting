MONGO_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/raft_booking?retryWrites=true&w=majority"
SECRET_KEY="change-this-to-a-random-string"
FLASK_ENV=production

# NOTE: Do not commit real credentials. Copy this to `.env` and fill values for local development.